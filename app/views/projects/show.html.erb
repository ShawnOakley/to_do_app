<h2><%= @project.title %></h2>
</br>
</br>
    <% @project.items.each do |item| %>
    Items to do:
    <ul>
      <li>
        <%= item.title %>
      </li>
    </ul>
    <%end%>

    <form action='<%= team_url(@project.team_id) %>' methd='get'>
      <input type='submit' value="Return to Team">
    </form>
